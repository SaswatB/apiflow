const state = { // TODO: implement projects
  flows: {},
  flowFolders: {},
  requests: {},
  requestFolders: {},
  openProcedure: ""
}

const mutations = {
  setFlows (state) {
    state.main--
  },
  INCREMENT_MAIN_COUNTER (state) {
    state.main++
  }
}

const actions = {}

export default {
  state,
  mutations,
  actions
}
